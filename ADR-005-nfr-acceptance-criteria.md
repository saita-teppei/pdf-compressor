# ADR-005: 非機能要件・受入基準・メモリ制限・ベンチマーク基盤

- Status: Accepted
- Date: 2026-09-01
- Related: ADR-001, ADR-002, ADR-004

## Context

ADR-002のBenchmark Planは測定「項目」を列挙するが、**合否閾値（成功の定義）**が無かった。閾値が無いとベンチマーク結果を評価できず、エンジン選定も探索モードの上限設計もできない。また、大容量PDFのメモリ対策が「入力＋出力＋作業メモリが同時に必要」という課題提起に留まっていた。

本ADRで、対象デバイス別のNFR初期値・受入基準・メモリ制限方式・ベンチ基盤を確定する。

## Decision

### 1. 非機能要件（受入基準の初期値）

| 指標 | デスクトップ目標 | モバイル目標 | 備考 |
|---|---|---|---|
| 最大入力ファイルサイズ | 100 MB | 30 MB | 超過は処理前に拒否 |
| 最大ページ数（目安） | 2,000 | 500 | サイズと二重ガード |
| 削減率（スキャンPDF/既定プリセット） | ≥ 50% | 同左 | 内容依存のため保証でなく目標 |
| 処理時間（10MBスキャンPDF, 既定） | ≤ 10 秒 | ≤ 25 秒 | 中位端末想定 |
| 初期ロード（WASM込, 初回） | ≤ 5 秒 | ≤ 10 秒 | 2回目以降はキャッシュ |
| ピークメモリ | 入力サイズの概ね ≤ 4〜6倍 | 同左 | 上限ガードの根拠 |
| 画質（既定プリセット） | SSIM ≥ 0.90 | 同左 | 客観指標で測定 |
| ページ数維持 | 100% | 100% | 必須 |
| 出力の可読性 | 標準ビューアで開ける | 同左 | 出力後に軽検証 |

これらは**ベンチマークで妥当性を検証・調整する初期値**であり、合否閾値として用いる。

### 2. メモリ制限方式

- **入力ファイルサイズとページ数で上限ガード**する（上表の値）。上限超過は処理前に拒否し明示メッセージを表示。
- 端末プロファイル（`navigator.deviceMemory` / モバイル判定）に応じて上限を出し分ける。
- OOM検知時はグレースフルにエラー復帰し、タブクラッシュを避ける。
- 処理完了・失敗いずれでもエンジンの `dispose()` でメモリを明示解放する。

### 3. ベンチマーク基盤

- **テストPDFコーパス**: ADR-002の分類（スキャン白黒/グレー/カラー、写真、テキスト、混在、フォント/ベクター、日本語縦書き）を、権利上クリーンな素材で用意する。
- **客観画質指標**: SSIM（必要に応じPSNR）を測定手段として採用。ページ画像をレンダリングして比較。
- **計測ハーネス**: 出力サイズ・削減率・処理時間・初期ロード・ピークメモリ・ページ数維持・テキスト抽出可否等を自動計測し、結果を保存する。
- **回帰**: エンジン/パラメータ変更時に上表閾値で合否判定できるようにする。
- **実測メモ（docs/benchmark/RESULTS.md）**: SSIM≥0.90 は達成可能だが**境界はコンテンツ依存**（scan-colorは72dpiで0.90、scan-grayは100dpi推奨、写真/混在はより高DPI）。画質指標の合否は「既定プリセット」ではなく**コンテンツ別**に評価する方針とする（ルーティング実証で確定）。
- **ブラウザ実測反映（RESULTS.md 第6回）**: 実ブラウザ(Chromium) + Web Worker で計測。処理時間 NFR（10MB スキャン ≤10s desktop）は達成（scan-color/photo medium で ≤1.3s）。頁維持 64/64、削減率・SSIM は Node 実測と一致（移植等価）。ピークメモリは `measureUserAgentSpecificMemory` が COI 必須で使用不可のため WASM 線形メモリ byteLength を代理採取（GS は ~64–80MB でほぼ固定）。**未達確認事項**: モバイル実機の処理時間、実ネットワーク配信での初回ロード（PoC は localhost・CPUエミュレーションのため）。

## Consequences

### Positive

- ベンチマーク結果を客観的に合否判定できる
- メモリ枯渇を事前ガードで予防でき、可用性が上がる

### Negative

- 権利上クリーンなコーパスと計測ハーネスの準備コストが必要
- 初期NFR値は実測で調整が入る前提（暫定確定）

## Revisit Conditions

- ベンチマーク実測でNFR値が非現実的と判明した場合
- 対象端末・想定ファイルサイズの前提が変わった場合
