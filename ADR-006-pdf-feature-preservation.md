# ADR-006: PDF機能保全ポリシー

- Status: Proposed
- Date: 2026-09-01
- Related: ADR-002, ADR-005

## Context

ADR-002のBenchmark Planは「テキスト抽出可否」「フォント維持」「リンク・しおり・注釈の維持」などを測定項目に挙げるが、**プロダクトとして何を保証し、何をベストエフォートとし、何を破棄するか**の意思決定が無かった。

Ghostscript等による再生成型圧縮は、PDF内の一部要素を変化・喪失させうる。特に**電子署名は再生成で必ず無効化される**。ユーザーが意図せず情報や機能を失うことを防ぐため、影響を**処理開始前に明示**する方針を採る。

## Decision

### 1. 処理開始前の明示

- `inspect()`（ADR-002のCompressionEngine）で入力PDFに含まれる要素を検出し、選択中の圧縮設定で**「維持される要素」「ベストエフォートの要素」「破棄・変化しうる要素」を処理開始前にユーザーへ提示**する。ユーザーが確認・同意してから圧縮を実行する。

### 2. 保全マトリクス（初期方針）

| 要素 | 分類 | 補足 |
|---|---|---|
| ページ数 | 維持 | 必須（ADR-005） |
| テキスト・フォント（電子文書） | ベストエフォート | エンジン・設定依存。維持を優先 |
| ベクター・図形 | ベストエフォート | ラスタライズ設定で変化しうる |
| 埋め込み画像 | 変化 | ダウンサンプリング・再圧縮が目的 |
| **電子署名** | **破棄** | 再生成で必ず無効化。強く警告 |
| フォーム（AcroForm/XFA） | 破棄しうる | XFAは特に非対応の可能性 |
| 注釈・コメント | ベストエフォート | エンジン依存 |
| しおり・リンク | ベストエフォート | エンジン依存 |
| タグ付き構造（PDF/UA, アクセシビリティ） | 破棄しうる | 再生成で失われやすい |
| PDF/A適合 | 破棄しうる | 適合性は保証しない |
| レイヤ（OCG） | 破棄しうる | エンジン依存 |
| 添付ファイル | 破棄しうる | エンジン依存 |
| XMP/文書メタデータ | 選択可（除去も機能） | 除去はプライバシー用途にも使える |

分類はベンチマーク（ADR-005）で実測して確定・調整する。

### 3. メタデータ除去

- メタデータ（作成者・XMP等）の除去を任意機能として提供しうる（プライバシー用途）。デフォルト挙動は要決定。

## Consequences

### Positive

- ユーザーが失う要素を事前に把握でき、意図しない喪失を防げる
- 署名無効化のような重大な副作用を明示できる

### Negative

- `inspect()` に要素検出の実装コストが必要
- エンジンごとに実際の維持可否が異なるため、マトリクスはエンジン別に持つ必要が生じうる

## Revisit Conditions

- ベンチマークで各要素の実際の維持可否が判明した場合
- 用途別エンジン分離（ADR-002）が決まり、マトリクスをエンジン別にする場合
