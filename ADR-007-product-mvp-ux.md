# ADR-007: プロダクトMVPスコープ・UX・縮退・免責

- Status: Proposed
- Date: 2026-09-01
- Related: ADR-001, ADR-005, ADR-006

## Context

ADR-001/002はアーキテクチャ中心で、プロダクト仕様（機能・UX・エラー時挙動・法務）がほぼ未記載だった。完全クライアント側処理のためサーバーフォールバックが無く、非対応環境やエラーの縮退をアプリ側で担保する必要がある。主用途は部内利用。

## Decision

### 1. MVPスコープ（機能）

以下をMVPに含める。

- **モバイル/PC両対応**（レスポンシブUI）
- **複数ファイル処理**
- **ドラッグ&ドロップ**によるファイル投入
- **進捗表示**（determinate %／indeterminate）
- **キャンセル**（処理中のジョブを中断）
- **一括ダウンロード**（zipでまとめてDL）
- 探索/圧縮結果の**比較UI**（サイズ・削減率・処理時間の比較、探索モードと連携）
- **アクセシビリティ(a11y)** の確保
- **国際化(i18n)**（日本語／英語）

残タスク: 出力ファイル名規約、圧縮前後プレビュー、再圧縮の扱い。

#### 圧縮モード（プリセット / マニュアル）

- **プリセットモード（既定）**: `max`(72dpi) / `balanced`(100dpi, 既定) / `quality`(150dpi)。第3回実測（RESULTS.md）に基づくDPI値。ADR-008の `CompressionOptions.preset` に対応。
- **マニュアルモード**: ユーザーが設定を直接指定する。公開する設定は実測で実効性が確認できたものに限る:
  - **DPI**: 72–300（主コントロール。スライダ/数値）。画像の主レバー。
  - **画質(quality)**: 1–100（副コントロール）。Ghostscript では QFactor 経由で実装（`-dJPEGQ` は無効だった。ADR-008）。SSIM境界をまたぐ微調整に有効。
  - **カラー**: color / grayscale / monochrome。
  - **メタデータ除去**: on/off（プライバシー。ベストエフォート, ADR-006）。
- **リアルタイム確認**: 設定変更後「圧縮」で**実際に生成した実サイズ・削減率**を表示（ADR-001）。探索モード（ADR-008）では複数設定を比較UIで一覧。
- **入力ガード**: DPIは範囲クランプ。SSIMがコンテンツ依存で低下しうる積極設定（例: 写真に72dpi）では注意表示（RESULTS.mdの傾向に基づく）。

### 2. エラー処理・縮退（サーバーフォールバック無し前提）

- **機能検出**: WASM／必要機能を検出し、非対応ブラウザには明示メッセージを表示（サーバー処理へは回さない）。
- **タイムアウト**: 長時間ジョブ・ハングを打ち切る（悪性/破損PDF対策＝可用性、ADR-004）。
- **キャンセル**: ユーザーが任意に中断可能。
- **部分失敗ハンドリング**: バッチ処理で1件失敗しても全体を止めず、失敗ファイルを明示。
- **出力軽検証**: 出力PDFが開けること・ページ数維持を確認してからDL提供（ADR-005）。
- **肥大化ガード（"don't grow"）**: 出力が入力以上のサイズになった場合は**元ファイルを採用**し、UIで「これ以上圧縮できません」と提示する。実測（RESULTS.md）で、テキスト主体/縦書きなど元が小さいPDFはpdfwrite再生成でむしろ肥大化することを確認したため必須。

残タスク: 対応ブラウザ最小マトリクスの確定。

### 3. プライバシー・免責

- PDFは**一切サーバー送信せず**クライアント内で処理することを告知する。
- 主用途は部内利用であり、**機微情報は扱わない方針**とする。機微情報が扱われた場合について本サービスは責任を負わない旨を明記する。
- テレメトリ/解析を導入する場合も、**PDF内容は絶対に送信しない**データ境界を明記する。

## Consequences

### Positive

- クライアント側の制約下でも、非対応・エラー時に破綻しないUXを提供できる
- プライバシー訴求（無送信）とスコープ・免責が明確になる

### Negative

- a11y・i18n・比較UI・バッチ縮退など実装項目が多く、MVPでも相応の工数
- サーバーフォールバックが無いため、非対応環境は「利用不可」を明示するしかない

## Revisit Conditions

- 部外/一般公開へ用途が広がる場合（免責・プライバシー方針の見直し）
- 機微情報の取り扱いが要件化した場合
