# AGPLv3 コンプライアンス運用

- Date: 2026-09-01
- Related: ADR-003（ライセンス方針）, LICENSE（AGPLv3全文）

本書は ADR-003 で決定した「AGPLv3を許容し、アプリ全体をソース公開する」方針を、実際に順守するための運用手順を定める。Ghostscript WASM を配信・結合して利用する本サービスは、AGPLv3 の「ネットワーク越しの提供」条項の対象になるため、以下を継続的に満たす。

## 1. 対応するソース（Corresponding Source）の提供

AGPLv3 §13 により、ユーザーがネットワーク経由でサービスを利用する場合、**そのユーザーへ対応するソースを提供**する義務がある。

- **公開リポジトリ**: アプリケーション全体（フロントエンド・WASMグルーコード・ビルド設定を含む）を公開リポジトリで配布する。
- **アプリ内導線**: 配信されるWebアプリのフッター等に「ソースコード」リンクを常設し、稼働中バージョンに対応するソースへ到達できるようにする。
- **対応するソースの範囲**: 実行に必要なスクリプト・ビルド手順・依存の取得方法を含む（ビルドを再現できること）。

## 2. 稼働版とソースの対応（バージョン固定）

- 配信ビルドに **バージョン識別子（gitコミットハッシュ等）** を埋め込み、アプリ内に表示する。
- その識別子で公開リポジトリの**対応するコミット／タグ**へ到達できるようにする。
- WASMバイナリはコンテンツハッシュ付きで配信し（ADR-004）、どのソースから生成したかを追跡可能にする。

## 3. ライセンス告知・著作権表示

- リポジトリ直下に `LICENSE`（AGPLv3全文）を置く（配置済み）。
- Ghostscript 等の著作権表示・ライセンス通知を保持し、`THIRD-PARTY-NOTICES.md`（下記）に集約する。
- 改変を加えた場合、AGPLv3 §5 に従い変更点を明示する。

## 4. 依存ライセンスの互換性点検

AGPLv3 は copyleft が強いため、**AGPLと結合可能なライセンスの依存のみ**を使用する。

- 新規依存の追加時に必ずライセンスを確認し、AGPLv3 との結合可否を判定する。
- 判定結果を `THIRD-PARTY-NOTICES.md` に記録する。
- 判定の目安:

| 依存のライセンス | AGPLv3との結合 | 備考 |
|---|---|---|
| AGPL-3.0 / GPL-3.0 | 可 | 同系統 |
| LGPL, MIT, BSD, Apache-2.0, ISC | 可（寛容） | 告知保持が必要 |
| GPL-2.0 **only** | 不可の恐れ | v3非互換。要個別判断 |
| プロプライエタリ／再配布制限あり | 原則不可 | 採用しない |

※ Apache-2.0 は GPLv3/AGPLv3 と一方向で互換（Apache→AGPL方向は可）。GPLv2-only は非互換のため避ける。

## 5. THIRD-PARTY-NOTICES の維持

`THIRD-PARTY-NOTICES.md` を作成・更新し、各依存について以下を記録する。

- 依存名・バージョン
- ライセンス
- AGPLとの結合可否（点検結果）
- 著作権表示・必要な通知文

（初版はビルド構成が固まった時点で生成する。依存確定前の現段階ではテンプレートのみ。）

## 6. チェックリスト（リリース前）

- [ ] `LICENSE`（AGPLv3全文）が存在する
- [ ] アプリ内にソースコードへのリンクがある
- [ ] 稼働版のバージョン識別子が表示され、対応コミットへ辿れる
- [ ] 全依存のライセンスが点検され、AGPL非互換が無い
- [ ] `THIRD-PARTY-NOTICES.md` が最新
- [ ] Ghostscript等の著作権表示・通知を保持している

## Revisit

- クローズドソース化・SaaS商用展開が必要になった場合は ADR-003 を再検討（AGPL維持不可なら Artifex 商用ライセンス取得）。
